// Author : Phenzi Blasio
// Program: Learning Data types, constants and variables 
#include <stdio.h>
int main (){
    int a; // variable declaration
    // making a character data type 
    char = A;
    float real_number;
    // stray from defining variable names as hardcoded keywords in the sys func 
    
    // you can also do %c to pass the character from the 
    a = 12; // intialize 
    printf("%f":,real_number); 
    
    // datatype space variable name 
    // printf is a function that can take in more than one argument, one of the agruments just has to be a string literal "" 
    return 0; 
}

