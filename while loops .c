// programmer: phennywenny
// program: while loops and 
#include <stdio.h>
#include <stdlib.h>

int main(){
    int index = 1;
    while(index < = 5){
            // while run as long as the condition is true, nothing gets printeed hehe 
            printf("%d\n", index");
            index++;
            // all the^^ is add one to the stored variable will stop when condition is met 
    // now do while loop, it will exceute the code then excute the condition of the while loop 
    
    }
    
    printf("%d\n", index);
    
    
    
    
    
    
    
    
    
    return 0;   
}
