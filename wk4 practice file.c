#include <stdio.h>
#include <stdlib.h>

int main ()
{ 
    printf("%f", pow(2,3);
    
    /* this is a commenting function */ 
    /* this is the syntax for blue comments hehehe*/ 
    
    printf("User enter your age in a numerical value");
    int userage;
    scanf("%d",&userage);
    /* apmhersand & then variable this is a pointer*/
    printf(" You are %d years old", age);
    /* %lf is to enter a float value for the scanf function 
    char grade;
    printf("enter ur grade:");
    scanf("%c",&grade);
    printf("your grade is %c", grade);
    return 0;
    */
    /* arrays, characters being able to store for vaiables 
    */ 
    char name[20];
    printf("enter your name:");
    /* %s is a string input character for the scanffucntion, but you cannot get strings with spaces 
    fgets is another function that grabs a whole line of text */
    scanf("%s",&name)
    /* fgets(variable, # of characters needed to have the fgets function run*/
    fgets(name,20,stdin); 
    
    
    scanf
    return 0;
}
